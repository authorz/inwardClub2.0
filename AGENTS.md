# AGENTS.md

本文件是 InwardClub 2.0 项目中 Codex/Agent 必须遵守的行为规则，作用范围为整个仓库。

这些规则用于减少常见的 AI 编码错误，整体上偏向谨慎而非速度；对于明显简单的任务，可以按实际复杂度精简过程，但不得违反核心约束。

## 核心称呼

每次面向用户的回复，包括过程更新、提问和最终答复，都必须先称呼用户为“老大”。

## 开发责任与多 Agent 协作

- Codex 全权负责本项目的分析、实现、验证和交付，不再把开发任务派发给 Claude，也不再以“Claude 开发、Codex 验收”的方式协作。
- Claude 已退出本项目；不得将 Claude 作为后续任务的执行者、审查者或依赖方。
- 任务确有可独立并行的子问题，且使用多 Agent 能带来明确收益时，由 Codex 调用自身的多 Agent 能力；主 Agent 仍对任务拆分、结果整合、冲突处理和最终验证负责。
- 简单任务或强耦合任务由 Codex 直接完成，不为使用多 Agent 而使用多 Agent。

## 1. 编码前先思考

不要默默假设，也不要掩盖不确定性。开始实现前：

- 明确说明关键假设；无法可靠判断时先询问。
- 存在多种合理解释时，列出差异和影响，不要静默选择。
- 如果有明显更简单的方案，应主动说明。
- 如果需求存在会实质影响结果的歧义，停止实现，指出具体疑点并询问。
- 在开始修改前说明预期改动范围、取舍和验证标准。

## 2. 简单优先

只实现满足当前需求所必需的最小改动：

- 不添加用户没有要求的功能。
- 不为一次性逻辑创建无必要的抽象。
- 不添加未被要求的通用化、可配置化或扩展点。
- 不为实际不可能发生的场景堆叠错误处理。
- 如果实现明显可以大幅缩短，应先简化再交付。

提交前自问：资深工程师是否会认为这个实现过度复杂？如果会，继续简化。

## 3. 接口定位顺序

当用户提供接口路径或要求排查某个接口时，必须优先从路由注册定位代码：

1. 根据接口前缀和 HTTP 方法找到对应的 Router/路由注册。
2. 从路由确认实际绑定的 handler。
3. 再沿 handler → service → repository/外部依赖追踪调用链。
4. 只有路由链路无法提供足够信息时，才进行更大范围的全局搜索。

不得跳过路由注册直接泛读模块或全项目搜索，以减少无意义的代码阅读和错误定位。

## 4. 外科手术式修改

只修改完成当前任务必须触及的内容，并保护用户已有改动：

- 不顺手改进相邻代码、注释或格式。
- 不重构与当前需求无关且没有损坏的代码。
- 遵循现有项目风格，即使个人偏好不同。
- 发现无关的废弃代码时，只在交付说明中指出，不擅自删除。
- 删除或整理仅限于本次改动新产生的无用 import、变量、函数和文件。
- 工作区已有改动默认属于用户；不得覆盖、回退或混入无关修改。

判断标准：每一处变更都必须能直接追溯到用户当前需求。

## 5. 目标驱动执行

把任务转化为可验证的目标，并持续执行到验证完成：

- “添加校验”应转化为：先覆盖无效输入，再使测试通过。
- “修复 Bug”应转化为：先复现问题，再修复并验证回归。
- “重构”应转化为：确认重构前后的行为和测试保持一致。

多步骤任务开始前给出简短计划，格式如下：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

执行过程中根据结果更新计划；不要以“应该能工作”代替实际验证。

## 6. 完成标准

交付前必须：

- 检查变更范围，确保没有无关修改。
- 运行与风险相匹配的测试、构建、lint 或静态检查。
- 明确报告已验证内容、未能验证内容及其原因。
- 不把构建通过等同于运行时行为正确；涉及接口、数据库或跨端契约时，应验证数据形状和关键流程。
- 如果发现阻断问题，不得隐瞒或宣称任务已完成。

这些规则的目标是：减少无关 diff、过度设计、未经验证的实现，以及因未提前澄清而导致的返工。
