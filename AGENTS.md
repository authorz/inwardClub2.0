# AGENTS.md

本文件是 InwardClub 2.0 项目中 Codex/Agent 必须遵守的行为规则，作用范围为整个仓库。

这些规则用于减少常见的 AI 编码错误，整体上偏向谨慎而非速度；对于明显简单的任务，可以按实际复杂度精简过程，但不得违反核心约束。

## 核心称呼

每次面向用户的回复，包括过程更新、提问和最终答复，都必须先称呼用户为“老大”。

## 核心路由定位规则

当用户提供任何路由地址时，无论是页面路由还是接口路由，都必须先从项目注册路由的地方定位实际代码。不得跳过路由注册，直接全局搜索页面、组件、handler、service 或业务关键词；只有路由注册链路无法提供足够信息时，才允许扩大搜索范围。

## 核心提交规则

每完成一轮代码、配置、文档或资源改动并通过必要验证后，必须立即创建一次 Git 提交并 push；不得累计多轮已完成改动后再统一提交，也不得只提交不推送。

所有 Git 提交说明必须使用中文，并尽可能清楚、具体地描述本次修改涉及的内容；不得使用含义模糊、无法体现实际改动的笼统说明。

## 核心打包规则

- 当用户说“打包”、要求生成服务端部署包或编译服务端二进制时，必须调用仓库根目录的 `./scripts/package-server.sh`，不得再手写临时 `go build`、`tar` 等命令绕过该脚本。
- 脚本默认生成 Linux AMD64 的 API、Worker、Migrate 三个静态二进制、构建信息、SHA-256 清单和 `.tar.gz` 压缩包；交付时必须报告压缩包路径和 SHA-256。
- 用户明确要求其他目标平台或产物结构时，应先修改并验证该脚本，再通过脚本完成打包；不得在脚本之外另建一套打包流程。

## 核心金额与金币单位规则

- 人民币金额统一使用整数“分”存储和传输，字段必须使用 `Cent` / `_cent` 后缀；例如 `3900` 表示 `39.00 元`。不得使用浮点数存储或计算人民币金额。
- 金币是独立的整数资产，钱包余额和金币流水的单位均为“金币个数”；`1 金币 = 1 元 = 100 分`。
- 禁止直接比较、相加或相减人民币分与金币个数。金币支付、退款和余额预判必须调用项目统一换算方法，不得在业务代码中散落 `/ 100` 或 `* 100`。
- 金币支付只支持整元订单；人民币分金额不能被 100 整除时，不得取整或四舍五入，必须拒绝金币支付。
- 微信支付接口使用人民币分；支付单和退款单始终记录人民币分，金币账本只记录实际变动的金币个数。
- 活动票档配置的 `stock_quantity = 0` 表示不限量。对外接口和前端必须明确区分“不限量”与“有限库存已售罄”，不得依赖含义不清的 `0` / `-1` 魔法值进行业务判断。

## 开发责任与多 Agent 协作

- Codex 全权负责本项目的分析、实现、验证和交付，不再把开发任务派发给 Claude，也不再以“Claude 开发、Codex 验收”的方式协作。
- Claude 已退出本项目；不得将 Claude 作为后续任务的执行者、审查者或依赖方。
- 任务确有可独立并行的子问题，且使用多 Agent 能带来明确收益时，由 Codex 调用自身的多 Agent 能力；主 Agent 仍对任务拆分、结果整合、冲突处理和最终验证负责。
- 简单任务或强耦合任务由 Codex 直接完成，不为使用多 Agent 而使用多 Agent。

## 1. 编码前先思考

不要默默假设，也不要掩盖不确定性。开始实现前：

- 明确说明关键假设；无法可靠判断时先询问。
- 存在多种合理解释时，列出差异和影响，不要静默选择。
- 如果有明显更简单的方案，应主动说明。
- 如果需求存在会实质影响结果的歧义，停止实现，指出具体疑点并询问。
- 在开始修改前说明预期改动范围、取舍和验证标准。

## 2. 简单优先

只实现满足当前需求所必需的最小改动：

- 不添加用户没有要求的功能。
- 不为一次性逻辑创建无必要的抽象。
- 不添加未被要求的通用化、可配置化或扩展点。
- 不为实际不可能发生的场景堆叠错误处理。
- 如果实现明显可以大幅缩短，应先简化再交付。

提交前自问：资深工程师是否会认为这个实现过度复杂？如果会，继续简化。

## 3. 页面与接口路由定位顺序

当用户提供页面路由、接口路由或要求排查某个路由时，必须优先从路由注册定位代码：

1. 先判断是页面路由还是接口路由，并找到项目中对应的路由注册入口。
2. 页面路由必须从应用路由配置、页面注册表或 Router 定义确认实际绑定的页面、模块或组件，再进入对应文件。
3. 接口路由必须根据接口前缀和 HTTP 方法，从 Router/路由注册确认实际绑定的 handler。
4. 完成路由绑定确认后，再沿页面依赖链或 handler → service → repository/外部依赖追踪调用链。
5. 只有路由注册链路无法提供足够信息时，才允许进行受限的补充搜索或更大范围的全局搜索。

不得跳过路由注册直接泛读模块、搜索业务关键词或全项目搜索，以减少无意义的代码阅读和错误定位。

## 4. 外科手术式修改

只修改完成当前任务必须触及的内容，并保护用户已有改动：

- 不顺手改进相邻代码、注释或格式。
- 不重构与当前需求无关且没有损坏的代码。
- 遵循现有项目风格，即使个人偏好不同。
- 发现无关的废弃代码时，只在交付说明中指出，不擅自删除。
- 删除或整理仅限于本次改动新产生的无用 import、变量、函数和文件。
- 工作区已有改动默认属于用户；不得覆盖、回退或混入无关修改。

判断标准：每一处变更都必须能直接追溯到用户当前需求。

## 4.1 默认禁止卡片式页面布局

- 页面默认采用平面信息层级，通过留白、字体、对齐和分隔线组织内容。
- 除非用户明确要求，否则不得用圆角、有色背景、描边或阴影容器包裹整个表单、列表、详情区或主要内容区。
- 不得为了“视觉丰富”把页面拆成多个卡片，也不得出现卡片套卡片。
- 输入框、按钮、状态标签等功能控件可以保留自身必要的边界和选中状态；这不等同于使用卡片容器。
- 只有会员卡、票券、商品等业务对象本身具有明确卡片语义，或用户明确指定卡片样式时，才允许使用卡片。

## 4.2 iOS 风格玻璃材质规则

- 玻璃材质只用于需要悬浮层级或持续导航语义的区域，例如 TabBar、浮动操作区、底部弹层和用户明确要求的重点模块；不得把毛玻璃当作所有容器的默认装饰。
- 深色背景上的玻璃必须以“高透明中性色 + 背景模糊 + 适度饱和度”为基础。可从 `rgba(20, 24, 30, A)`（`A = 0.24–0.42`）、模糊 `28–44rpx`、饱和度 `125%–160%` 的范围内校准，再根据实际底图调整；不得用高不透明乳白色模拟磨砂塑料。
- 材质层级依靠克制的 `1rpx` 半透明边缘、顶部高光和短距离低强度阴影建立。不得堆叠粗描边、强发光、长投影、底部白边或多层伪立体边线。
- 非选中控件应主动收敛，使用约 `0.60–0.72` 的白色或图标透明度；选中控件才允许使用更亮的玻璃控制面、完整图标不透明度和更高文字对比度。状态差异不得只依赖颜色。
- 玻璃上的正文和关键标签必须满足 WCAG AA 对比度；背景内容过亮或过杂时，应先调整玻璃 tint、brightness 或局部遮罩，不能依靠加粗文字掩盖可读性问题。
- 只调整玻璃视觉时，不得顺带改变组件高度、底部距离、安全区处理、点击热区或页面内容避让；确需调整布局时，必须单独说明并验证不会遮挡内容。
- 玻璃效果必须使用 WXSS/CSS/SVG 等代码实现，不得引入位图纹理或截图伪造。必须保留不支持 `backdrop-filter` 时仍可辨识、可操作的半透明背景兜底。

## 5. 目标驱动执行

把任务转化为可验证的目标，并持续执行到验证完成：

- “添加校验”应转化为：先覆盖无效输入，再使测试通过。
- “修复 Bug”应转化为：先复现问题，再修复并验证回归。
- “重构”应转化为：确认重构前后的行为和测试保持一致。

多步骤任务开始前给出简短计划，格式如下：

```text
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]
```

执行过程中根据结果更新计划；不要以“应该能工作”代替实际验证。

## 6. 完成标准

交付前必须：

- 检查变更范围，确保没有无关修改。
- 运行与风险相匹配的测试、构建、lint 或静态检查。
- 明确报告已验证内容、未能验证内容及其原因。
- 不把构建通过等同于运行时行为正确；涉及接口、数据库或跨端契约时，应验证数据形状和关键流程。
- 如果发现阻断问题，不得隐瞒或宣称任务已完成。

## 7. 本地运行环境交付

- 运行时行为受影响时，Codex 必须在交付前主动启动或重启相关 Go 服务，不把常规重启步骤留给用户。
- 新增数据库迁移时，Codex 必须主动执行 `go run ./cmd/migrate up` 并确认迁移版本。
- 需要 MySQL、Redis 等本地依赖时，由 Codex 启动并检查健康状态。
- 交付给用户时，本地环境应处于可直接测试状态；用户只负责最终测试和验收。
- `server/.env` 含 shell 元字符，不得直接 `source`；必须用不会执行值内容、且不会输出密钥的方式加载。

## 8. Git 提交与推送

- 每次完成代码、配置、文档或资源修改并通过必要验证后，都必须将本次改动提交到 Git，并立即推送到当前分支对应的远程仓库。
- 所有 Git 提交说明必须使用中文，并尽可能清楚、具体地描述本次修改涉及的内容；不得使用含义模糊、无法体现实际改动的笼统说明。
- 本仓库的 GitHub 远程操作必须使用 SSH 443 端口（`ssh://git@ssh.github.com:443/authorz/inwardClub2.0.git`）；不得先尝试默认 SSH 22 端口或 HTTPS 推送。
- 提交前必须检查变更范围，避免把密钥、证书、`.env`、临时文件、构建产物或无关改动写入仓库。
- 只有 `commit` 和 `push` 均成功后，才能向用户宣告本次修改已经交付；若推送失败，必须说明原因并继续尝试安全的可用方式。

这些规则的目标是：减少无关 diff、过度设计、未经验证的实现，以及因未提前澄清而导致的返工。
