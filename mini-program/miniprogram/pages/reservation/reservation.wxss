/* bottom padding keeps the last table clear of the floating action + custom tabbar */
.rsv { padding: 0 24rpx; padding-bottom: calc(236rpx + env(safe-area-inset-bottom)); box-sizing: border-box; }

/* Custom navigation avoids the separator rendered inside WeChat's native bar. */
.rsv__nav { background: var(--ic-white); }
.rsv__nav-bar {
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}
.rsv__nav-title { font-size: 34rpx; font-weight: 700; color: var(--ic-black); }

/* ---- selectors ---- */
.rsv__filters {
  height: 88rpx;
  display: flex;
  align-items: center;
  margin: 0 -24rpx;
  padding: 0 24rpx;
  background: var(--ic-white);
  border-bottom: 1rpx solid var(--ic-line);
  box-sizing: border-box;
}
.rsv__field--hover { opacity: 0.7; }
.rsv__filter { height: 88rpx; display: flex; align-items: center; gap: 12rpx; min-width: 0; }
.rsv__filter--store { flex: 1.05; }
.rsv__filter--hours { flex: 1; }
.rsv__filter-divider { width: 1rpx; height: 32rpx; margin: 0 22rpx; flex: none; background: var(--ic-line); }
.rsv__filter-text { flex: 1; min-width: 0; font-size: 26rpx; color: var(--ic-text-2); }
/* location icon — same box as clock/person icons */
.rsv__ic { width: 36rpx; height: 36rpx; flex: none; display: block; }
.rsv__filter .rsv__ic, .rsv__filter .ic-ic { width: 32rpx; height: 32rpx; color: var(--ic-text-3); }
.rsv__caret { width: 12rpx; height: 12rpx; flex: none; border-right: 2rpx solid var(--ic-text-3); border-bottom: 2rpx solid var(--ic-text-3); transform: rotate(45deg); margin: -6rpx 4rpx 0 0; }

.rsv__activity-banner {
  position: relative;
  height: 136rpx;
  margin: 20rpx 0 4rpx;
  padding: 0 24rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  overflow: hidden;
  border-radius: 12rpx;
  background: #1b1b1b;
  box-sizing: border-box;
}
.rsv__activity-banner--hover { opacity: 0.82; }
.rsv__activity-image, .rsv__activity-shade { position: absolute; inset: 0; width: 100%; height: 100%; }
.rsv__activity-shade { background: rgba(0, 0, 0, 0.48); }
.rsv__activity-copy { position: relative; z-index: 1; min-width: 0; flex: 1; display: flex; flex-direction: column; gap: 8rpx; }
.rsv__activity-label { color: #fff; font-size: 22rpx; line-height: 1; }
.rsv__activity-title { display: block; min-width: 0; color: #fff; font-size: 30rpx; font-weight: 700; }
.rsv__activity-more { position: relative; z-index: 1; flex: none; display: flex; align-items: center; gap: 10rpx; color: #fff; font-size: 24rpx; }
.rsv__activity-arrow { width: 12rpx; height: 12rpx; border-top: 2rpx solid currentColor; border-right: 2rpx solid currentColor; transform: rotate(45deg); }
.rsv__activity-empty {
  min-height: 88rpx;
  margin: 0 -24rpx;
  padding: 0 24rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24rpx;
  border-bottom: 1rpx solid var(--ic-line);
  background: var(--ic-white);
  box-sizing: border-box;
}
.rsv__activity-empty .rsv__activity-copy { flex-direction: row; align-items: baseline; gap: 18rpx; }
.rsv__activity-empty .rsv__activity-label { color: var(--ic-black); font-size: 28rpx; font-weight: 700; line-height: normal; }
.rsv__activity-empty-text { color: var(--ic-text-3); font-size: 24rpx; }

/* ---- legend ---- */
.rsv__legend { display: flex; gap: 44rpx; padding: 24rpx 12rpx 4rpx; }
.rsv__legend-item { display: flex; align-items: center; gap: 12rpx; }
.rsv__legend-text { font-size: 24rpx; color: var(--ic-text-2); }

.rsv__sec { min-height: 88rpx; padding: 16rpx 8rpx 8rpx; display: flex; align-items: center; justify-content: space-between; gap: 24rpx; }
.rsv__mine-link { min-height: 88rpx; display: flex; align-items: center; gap: 10rpx; font-size: 24rpx; color: var(--ic-text-3); }
.rsv__mine-link .ic-arrow { margin-left: 0; width: 12rpx; height: 12rpx; border-width: 2rpx; }

/* ---- table card ---- */
.rsv__table { margin-bottom: 24rpx; padding: 28rpx; }
.rsv__thead { display: flex; align-items: center; gap: 18rpx; }
.rsv__tname { font-size: 34rpx; font-weight: 700; color: var(--ic-black); }
.rsv__ttype { font-size: 24rpx; color: var(--ic-text-3); }
.rsv__tseats { font-size: 24rpx; color: var(--ic-text-2); }
.rsv__tupdated { flex: 1; text-align: right; font-size: 22rpx; color: var(--ic-text-4); }
.rsv__tmore { flex: 1; text-align: right; font-size: 24rpx; color: var(--ic-text-2); }
.rsv__tcaret { width: 16rpx; height: 16rpx; border-right: 3rpx solid var(--ic-text-3); border-bottom: 3rpx solid var(--ic-text-3); transform: rotate(45deg); transition: transform 0.2s; }
.rsv__tcaret.is-open { transform: rotate(-135deg); }

/* ---- compact dot summary ---- */
.rsv__dots { display: flex; gap: 14rpx; margin-top: 24rpx; }

/* ---- seat dots (legend + collapsed) ---- */
.rseat__dot { width: 26rpx; height: 26rpx; border-radius: 50%; box-sizing: border-box; }
.rseat__dot--free { background: #ececec; border: 1rpx solid #d8d8d8; }
.rseat__dot--reserved { background: #3a3a3a; }
.rseat__dot--selected { background: #111; box-shadow: 0 0 0 4rpx rgba(0, 0, 0, 0.12); }

/* ---- poker table ---- */
.rtable { position: relative; height: 420rpx; margin: 30rpx 8rpx 12rpx; }
.rtable__felt {
  position: absolute; top: 84rpx; left: 76rpx; right: 76rpx; bottom: 84rpx;
  border-radius: 240rpx;
  background: radial-gradient(120% 120% at 50% 40%, #2f5d40 0%, #1f4530 70%, #143020 100%);
  border: 14rpx solid #2a2724;
  box-shadow: inset 0 0 40rpx rgba(0, 0, 0, 0.45);
  overflow: hidden;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
}
.rtable__layout-bg {
  position: absolute;
  top: 36rpx;
  left: 54rpx;
  z-index: 0;
  width: calc(100% - 108rpx);
  height: calc(100% - 72rpx);
  display: block;
}
.rtable__suits { display: flex; gap: 18rpx; color: rgba(255, 255, 255, 0.28); font-size: 30rpx; }
.rtable__brand { margin-top: 12rpx; font-size: 24rpx; letter-spacing: 4rpx; color: rgba(255, 255, 255, 0.4); }

.rseat { position: absolute; z-index: 1; transform: translate(-50%, -50%); display: flex; flex-direction: column; align-items: center; }
.rseat__circle {
  width: 76rpx; height: 76rpx; border-radius: 50%; box-sizing: border-box;
  display: flex; align-items: center; justify-content: center;
  font-size: 30rpx; font-weight: 700;
  position: relative; overflow: visible;
}
.rseat__label { margin-top: 8rpx; font-size: 22rpx; color: var(--ic-text-3); white-space: nowrap; max-width: 120rpx; overflow: hidden; text-overflow: ellipsis; }
.rseat__nickname {
  display: block;
  width: 112rpx;
  margin-top: 8rpx;
  overflow: hidden;
  color: var(--ic-text-3);
  font-size: 22rpx;
  line-height: 30rpx;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.rseat--label-top { flex-direction: column-reverse; }
.rseat--label-top .rseat__label,
.rseat--label-top .rseat__nickname {
  margin-top: 0;
  margin-bottom: 8rpx;
}

/* reserved seat: avatar + gender badge inside the circle */
.rseat__avatar { width: 100%; height: 100%; border-radius: 50%; display: block; }
.rseat__avatar--brand { box-sizing: border-box; padding: 18rpx 8rpx; background: #fff; }
.rseat__initial { font-size: 28rpx; font-weight: 700; line-height: 1; }
.rseat__free-icon { width: 46rpx; height: 46rpx; display: block; }
.rseat__gender {
  position: absolute; right: -6rpx; bottom: -6rpx;
  width: 28rpx; height: 28rpx; border-radius: 50%;
  background: #fff; box-sizing: border-box; padding: 3rpx;
  box-shadow: 0 0 0 2rpx #fff;
}

.rseat--free .rseat__circle { background: #fff; border: 2rpx solid #d8d8d8; color: #111; }
.rseat--reserved .rseat__circle { background: #3a3a3a; color: #fff; }
.rseat--reserved .rseat__nickname { color: var(--ic-text-2); }
.rseat--reserved .rseat__nickname--mine { color: var(--ic-black); font-weight: 700; }
.rseat--selected .rseat__circle { background: #111; color: #fff; box-shadow: 0 0 0 6rpx rgba(0, 0, 0, 0.12); }
.rseat--selected .rseat__label { color: var(--ic-black); font-weight: 600; }

/* ---- floating waitlist action ---- */
.rsv__waitbar {
  position: fixed;
  left: 24rpx;
  right: 24rpx;
  bottom: calc(120rpx + env(safe-area-inset-bottom));
  z-index: 40;
}
.rsv__waitbtn {
  width: 100% !important;
  margin-left: 0 !important;
  margin-right: 0 !important;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  height: 84rpx;
  box-shadow: 0 8rpx 20rpx rgba(0, 0, 0, 0.14);
}

/* ---- confirm sheet body ---- */
.rsvc__row { display: flex; align-items: center; gap: 18rpx; padding: 20rpx 4rpx; }
.rsvc__val { font-size: 30rpx; color: var(--ic-black); min-width: 0; }
.rsvc__hint { margin-top: 18rpx; padding: 20rpx 24rpx; background: var(--ic-fill); border-radius: 14rpx; font-size: 24rpx; color: var(--ic-text-2); }
.rsvc__waitlist { display: flex; align-items: center; justify-content: center; gap: 8rpx; padding: 24rpx 0 4rpx; font-size: 24rpx; color: var(--ic-text-2); }
/* keep footer clear of the custom tabbar (100rpx) + safe area */
.rsvc__footer { display: flex; gap: 20rpx; margin-bottom: 100rpx; }
/* stable equal widths; override WeChat button default side margins, center text */
.rsvc__btn { flex: 1; margin-left: 0 !important; margin-right: 0 !important; width: auto; box-sizing: border-box; display: flex; align-items: center; justify-content: center; text-align: center; }
