<view wx:if="{{show}}" class="loading">
  <view class="loading__dots">
    <view class="loading__dot"></view>
    <view class="loading__dot"></view>
    <view class="loading__dot"></view>
  </view>
  <text wx:if="{{text}}" class="loading__text">{{text}}</text>
</view>
