VITE_APP_ID=inwardclub-admin-console
# 本地开发用同源相对路径，由 Vite dev server 代理到本地后端（见 vite.config.ts）。
# 避免 127.0.0.1 与 localhost 被判为跨域而触发 CORS 预检失败；生产由 Nginx 同源代理。
VITE_API_BASE_URL=/api/v2
VITE_AUTH_AUDIENCE=admin
VITE_ASSET_PUBLIC_DOMAIN=https://assets.inwardclub.com
